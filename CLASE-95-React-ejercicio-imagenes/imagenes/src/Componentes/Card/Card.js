import React from 'react';
import './Card.scss';

const Card = ({children}) => {

    return (
        <React.Fragment>
            {children}
        </React.Fragment>
      
    )
}

export default Card;